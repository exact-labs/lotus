[project]
name = "ecosystem"
version = "0.1.0"

[tasks]
# add maid task groups
run_daemon = { path = "services/daemon", script = "bun run src/daemon.ts" }
run_frontend = { path = "apps/frontend", script = "bunx vite --clearScreen false" }